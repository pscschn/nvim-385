# Plugin

This is a plugin. It is a descriptive unit that includes details like for a specific plugin.
Details for a specific plugin entail a config for plugin manager installation or plugin configuration for example. 

The overall load order of the plugin can be specified using the index file within the plugin directory. 

Change the prefix before ".index.lua" in 1.index.lua. 
The lower the number the higher the load priority. 

